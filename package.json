{
  "name": "rollback-netcode-lockstep",
  "version": "1.0.0",
  "description": "Sıfırdan rollback netcode ve deterministik lockstep: fixed-point simülasyon, girdi tamponu, state hash — sunucusuz, tek sayfada iki istemci",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
