{
  "name": "rollback-netcode-lockstep",
  "version": "1.0.0",
  "description": "Peer-to-peer deterministic rollback netcode simulation from scratch: state snapshots, speculative input prediction, rolling rollback frames, and fixed-point math.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
